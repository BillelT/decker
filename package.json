{
  "name": "figma-to-slides",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/shared",
    "packages/backend",
    "packages/plugin"
  ],
  "scripts": {
    "postinstall": "npm run build --workspace packages/shared",
    "build": "npm run build --workspace packages/shared && npm run build --workspaces --if-present",
    "pretest": "npm run build --workspace packages/shared",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "calibrate": "npm run calibrate --workspace packages/backend"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
